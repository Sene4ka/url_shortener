name: CI/CD

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "**" ]
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  test:
    name: Test & Coverage
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26.1'
          cache: true

      - name: Download dependencies
        run: go mod download

      - name: Run tests with coverage
        run: |
          mkdir -p coverage
          go test -v -race -coverprofile=coverage/coverage.out -covermode=atomic ./...
        shell: bash

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage/coverage.out
          fail_ci_if_error: false

  lint:
    name: Lint Code
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26.1'

      - name: Run golangci-lint
        uses: golangci/golangci-lint-action@v8
        with:
          version: '2.11.3'
          args: --timeout=5m

  format:
    name: Check Formatting
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26.1'

      - name: Check gofmt
        run: |
          test -z "$(gofmt -l . | tee /dev/stderr)"

  build-binaries:
    name: Build Binaries
    runs-on: ubuntu-latest
    needs: [test, lint, format]
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26.1'

      - name: Build for Linux
        run: |
          GOOS=linux GOARCH=amd64 go build -o shortener-linux-amd64 ./cmd/shortener
          GOOS=linux GOARCH=arm64 go build -o shortener-linux-arm64 ./cmd/shortener

      - name: Build for Windows
        run: |
          GOOS=windows GOARCH=amd64 go build -o shortener-windows-amd64.exe ./cmd/shortener

      - name: Build for macOS
        run: |
          GOOS=darwin GOARCH=amd64 go build -o shortener-darwin-amd64 ./cmd/shortener
          GOOS=darwin GOARCH=arm64 go build -o shortener-darwin-arm64 ./cmd/shortener

      - name: Upload binaries
        uses: actions/upload-artifact@v4
        with:
          name: shortener-binaries-${{ github.sha }}
          path: |
            shortener-*.exe
            shortener-*
          retention-days: 0

  build-docker:
    name: Build and Push Docker Image
    runs-on: ubuntu-latest
    needs: [test, lint, format]
    if: github.event_name == 'push' && (github.ref == 'refs/heads/main' || github.ref == 'refs/heads/master')
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
  
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Save Docker image as tar
        run: |
          docker save shortener:latest -o shortener-image.tar

      - name: Upload Docker image artifact
        uses: actions/upload-artifact@v4
        with:
          name: shortener-image-${{ github.sha }}
          path: shortener-image.tar
          retention-days: 0
  
      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
  
      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: |
            ${{ secrets.DOCKER_USERNAME }}/shortener:latest
            ${{ secrets.DOCKER_USERNAME }}/shortener:${{ github.sha }}
